# Getmore Evaluación 2

## Compile options

- `gulp` > compile all assets
- `gulp watch` > Watch and compile src styles and scripts
- `gulp styles` > Watch and compile src styles
- `gulp scripts` > Watch and compile src scripts
